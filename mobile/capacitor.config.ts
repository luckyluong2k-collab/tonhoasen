import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hoasenhome.phuly',
  appName: 'Hoa Sen Home Phủ Lý',
  webDir: 'www',
  server: {
    url: 'https://luckyluong2k-collab.github.io/tonhoasen/',
    cleartext: false
  }
};

export default config;
